#include <stdio.h>

/**
 * main - start point for program execution
 *
 * Return: 0 ends the execution of function
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
